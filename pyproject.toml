[project]
name = "jesterky-brian"
version = "0.1.0"
description = "Two-team Brian robot control panel"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.0",
    "requests>=2.31",
]
